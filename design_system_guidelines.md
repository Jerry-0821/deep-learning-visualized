# Deep Learning Visualized - Design System & Framework Guidelines

这份文档记录了本项目的前端页面架构、排版规范以及设计哲学。主要面向后续接手开发、维护或向系统中添加新 Topic（主题）的开发者和设计师。

## 1. 核心设计哲学 (Core Philosophy)
- **极致优雅 (Wide & Elegant)**：抛弃传统的“呆板博客”感，采用类似顶级现代学术期刊的“宽幅杂志排版”。
- **去卡片化 (Cardless Typography)**：文本内容**绝对避免**使用带白底、边框、阴影的“卡片 (Cards)”。依赖充足的留白、网格系统 (Grid) 和强烈的字体层级来划分内容区块。
- **高拓展性 (Fluid & Flexible)**：页面容器应当足够灵活。目前是“左侧大纲 + 中间宽幅阅读区”，未来如果需要在右侧添加“代码编辑器面板”、“侧边属性栏”等，现有的 Flex/Grid 布局可以轻松挤压或拓展，而不会崩溃。

---

## 2. 页面布局架构 (Layout Architecture)
整体框架使用弹性布局（Flexbox）和限制最大宽度来保持阅读体验。

- **顶栏 (Top Bar)**：毛玻璃效果（Backdrop-filter），固定在顶部，提供全局导航。
- **全局容器 (`.page-container`)**：最大宽度设为 `1440px`。这是一个完美的黄金比例，既能充分利用大屏空间，又不会让横向视线拉得过长。
- **左侧导航 (`.toc-sidebar`)**：宽度固定（如 `240px`），采用 `position: sticky` 随屏幕滚动。
- **主阅读区 (`.document-wrapper`)**：占据剩余空间 (`flex: 1`)，但拥有自己的最大宽度限制（如 `1000px`），确保文字行长在舒适的阅读范围内。如果未来右侧需要拓展面板，直接在此层级添加即可。

---

## 3. 字体与排版规范 (Typography)
本项目采用**双字体系统 (Dual-font System)**，将古典优雅与现代严谨完美融合。

### A. 衬线体 (Serif) - 负责“高级感与优雅”
- **字体**：`Instrument Serif`
- **使用场景**：仅用于所有的大标题（如 Hero Section 的 `<h1>`，以及内容流中的主节标题 `<h2>`）。
- **样式要求**：字重（Weight）通常保持 400，可以适当加上 `-0.01em` 的字母间距（Letter-spacing），让它看起来非常紧凑、学术。

### B. 无衬线体 (Sans-Serif) - 负责“信息传达与现代感”
- **字体**：`Inter` (Apple 极简风首选)
- **使用场景**：正文段落 (`<p>`)、小节标题 (`<h3>`)、UI 元素（按钮、导航链接、列表）。
- **样式要求**：
  - 正文 (`p`) 使用较轻的字重（`300` 或 `400`），行高（Line-height）设为 `1.8`，营造呼吸感。
  - 标签/小标题 (`h3`) 采用 `700` 字重，全大写 (`uppercase`)，并增加字间距 (`letter-spacing: 0.1em`)，作为页面的视觉锚点。

---

## 4. 色彩系统 (Color Palette)
严禁使用浏览器默认的死黑或纯白，所有颜色都必须经过调和。

- **背景色 (`--bg`)**: `#fcfbfd`（带有极微弱紫色倾向的牛奶白，比纯白更护眼、更显高级）。
- **主文本色 (`--text-primary`)**: `#23212c`（深灰紫色，**绝对不要使用纯黑色 `#000000`**）。
- **次文本色 (`--text-secondary`)**: `#585566`（用于正文说明、次要信息）。
- **强调色 (`--accent`)**: `#6c5ce7`（优雅的科技紫，用于高亮、激活状态、公式文字或细分隔线）。

---

## 5. 数学公式排版 (Math & LaTeX) - ⚠️ 极度重要
本项目是深度学习教程，数学公式的美感是核心。

- **渲染引擎**：强制使用 **MathJax** (渲染为 CHTML 或 SVG)。
- **规范**：**所有**的数学变量、公式、数值，甚至是正文里提到的一个变量名（比如 $x_1$ 或 $z$），都**必须**使用 LaTeX 语法包裹（例如 `\( x_1 \)` 或 `$$ z = \sum w_i x_i + b $$`）。
- **严禁**直接用普通英文字母代替数学变量，这会破坏学术严谨性和页面极高的质感。

---

## 6. 特殊结构组件指南 (Components)

### A. 互动画布区 (Interactive Canvas)
- 放置在 `<figure class="interactive-figure">` 容器内。
- 动画逻辑由 Canvas JS 驱动。**接手者注意**：JS 在更新周围的 DOM（如数值显示文本框）时，必须加上**空指针检查 (Null-check)**。因为未来页面可能会精简掉某些文字说明，如果 JS 强行更新不存在的 DOM 会导致整个动画崩溃卡死。

### B. 对比与多列内容 (Grid Content)
- 当遇到如“PROS（优点）”与“CONS（缺点）”，或“公式推导左右对比”时，**不要画表格，也不要加带边框的卡片**。
- **做法**：直接使用 CSS Grid (`display: grid; grid-template-columns: 1fr 1fr; gap: 40px;`)。通过无形的网格、充裕的 `gap`，以及优雅的 `<h3>` 标题来区分左右列内容，保持纯净的杂志排版感。

---

## 7. 总结与交接寄语
这套架构的核心魅力在于**“克制”**。
当你想要添加一个新功能、一个新版块时：
1. 不要急着加边框。
2. 不要急着加背景色。
3. 试着通过调整字体大小、粗细、间距（Margin/Gap），以及使用合理的颜色层级（Primary vs Secondary）来划分区域。

保持这种无边框的呼吸感和 LaTeX 公式的严谨感，这个网站就会一直维持着顶级学术教程的视觉体验。
