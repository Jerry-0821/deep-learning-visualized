# Attention Mechanism Intuition

This visualization follows embeddings, Query/Key/Value projections, attention scores, softmax weights, and context vectors.

- Open in Colab: `transfomer.ipynb`
- Outputs HTML: Yes, the notebook renders an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Attention Mechanism Intuition`; the deployed website uses the exported prototype in `public/prototypes/`.
