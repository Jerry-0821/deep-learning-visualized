# Dropout

This visualization explains random neuron masking and inverted dropout scaling.

- Open in Colab: `Dropout.ipynb`
- Outputs HTML: Yes, the notebook renders an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Dropout`; the deployed website uses the exported prototype in `public/prototypes/`.
