# Transfer Learning Intuition

This visualization shows how pretrained features can be reused and adapted to a smaller target task.

- Open in Colab: `transfer_learning_intuition_source.py`
- Outputs HTML: Yes, the Python source displays an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Transfer Learning Intuition`; the deployed website uses the exported prototype in `public/prototypes/`.
