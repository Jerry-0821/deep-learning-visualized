# Overfitting vs. Underfitting

This visualization lets learners adjust model complexity, data size, noise, and regularization.

- Open in Colab: `overfitting_top_controls_colab.py`
- Outputs HTML: Yes, the Python source displays an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Overfitting vs. Underfitting`; the deployed website uses the exported prototype in `public/prototypes/`.
