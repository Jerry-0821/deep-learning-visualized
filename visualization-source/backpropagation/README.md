# Backpropagation Intuition

This visualization follows error signals backward through cached forward values, gradients, and parameter updates.

- Open in Colab: `Backpropagation.ipynb`
- Outputs HTML: Yes, the notebook renders an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Backpropagation Intuition`; the deployed website uses the exported prototype in `public/prototypes/`.
