# Gradient Descent and Learning Rate

This visualization shows parameters moving across a loss surface and how learning rate changes the descent path.

- Open in Colab: `Gradient Descent.ipynb`
- Outputs HTML: Yes, the notebook renders an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Gradient Descent & Learning Rate`; the deployed website uses the exported prototype in `public/prototypes/`.
