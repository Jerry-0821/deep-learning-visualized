# Pooling and Downsampling

This visualization compares max pooling and average pooling across a sliding window.

- Open in Colab: `pooling_style_aligned_source.py`
- Outputs HTML: Yes, the Python source displays an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Pooling and Downsampling`; the deployed website uses the exported prototype in `public/prototypes/`.
