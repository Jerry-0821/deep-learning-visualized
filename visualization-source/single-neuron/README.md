# Single Neuron Forward Pass

This visualization shows how one neuron combines inputs, weights, bias, activation, and output.

- Open in Colab: `single_neuron.ipynb`
- Outputs HTML: Yes, the notebook renders an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Single Neuron Forward Pass`; the deployed website uses the exported prototype in `public/prototypes/`.
