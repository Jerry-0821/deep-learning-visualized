# Convolution Operation

This visualization shows how a filter slides across an input image and produces a feature map.

- Open in Colab: `convolution_operation_stepwise_source.py`
- Outputs HTML: Yes, the Python source displays an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Convolution Operation`; the deployed website uses the exported prototype in `public/prototypes/`.
