# Loss Functions

This visualization explains how prediction error becomes a scalar training signal.

- Open in Colab: `loss_functions_style_source.py`
- Outputs HTML: Yes, the Python source displays an interactive HTML visualization.
- Website connection: Source/reference code for the website topic `Loss Functions`; the deployed website uses the exported prototype in `public/prototypes/`.
